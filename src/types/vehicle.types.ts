export interface Vehicle {
  brand: string; // marca do veículo
  model: string; // modelo do veículo
  licensePlate: string; // placa do veículo
  rented: boolean; // indica se o veículo está alugado ou disponível
}
